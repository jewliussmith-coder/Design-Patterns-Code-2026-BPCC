Journal, Phase 2


Eager initialization creates the Singleton as soon as the class is loaded, while lazy initialization waits until the instance is actually needed. I would prefer lazy loading when creating the object takes a lot of resources or may not be needed. The trade-off is that the first call takes a little longer because the object has to be created.
