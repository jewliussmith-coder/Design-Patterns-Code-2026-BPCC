Journal

I relied on abstract types when I used EnemySpawner and Enemy in main. The main method does not need to know about ForestSpawner, DungeonSpawner, Goblin, Skeleton, or the equipment factories when creating enemies. Each spawner handles the concrete implementations internally. This makes the system easier to maintain because new spawners or enemy types can be added without changing the main game logic.

