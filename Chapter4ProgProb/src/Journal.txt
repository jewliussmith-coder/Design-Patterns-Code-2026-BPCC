Journal

Moving object creation into the SimpleEnemyFactory decouples the main method from the concrete Goblin and Skeleton classes. The main method only needs to work with the Enemy type and tell the factory which enemy it wants. If a new enemy like Slime is added later, the creation logic can be added to the factory instead of having to change the main game logic. This makes the code easier to maintain and expand.