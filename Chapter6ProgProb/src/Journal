Phase 2

Having the Command object responsible for its own undo logic makes the EditorApp simpler because the app does not need to know how each command is undone. The EditorApp only needs to keep track of the last command and call its undo() method. This also makes it easier to add new commands because each command can handle its own undo behavior.



