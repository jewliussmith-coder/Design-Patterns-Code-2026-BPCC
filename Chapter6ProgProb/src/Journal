Phase 5

This demonstrates the Composite Pattern because the MacroCommand groups multiple individual commands together and treats them as one command. The EditorApp does not need to know that the macro contains three different actions. It can execute or undo the entire group with one command, similar to pressing one button to perform several actions.





