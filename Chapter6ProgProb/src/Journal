Phase 3

A Stack is ideal for undo operations because it uses Last-In-First-Out (LIFO). This means the most recent command is undone first. If a Queue was used instead, the oldest command would be undone first, which would not work correctly for an undo system.




