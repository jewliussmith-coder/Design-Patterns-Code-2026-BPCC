Phase 4

InsertCommand only needs to store the text and position so it knows what was added and where to remove it. DeleteCommand needs to store the previous text because once the text is deleted, it is no longer in the editor. Without saving the deleted text before removing it, the command would not know what to put back during an undo.





